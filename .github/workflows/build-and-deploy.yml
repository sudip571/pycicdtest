name: Build, Publish & Deploy (Poetry)

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  build:
    name: Build & Publish Wheel
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Get source code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ">=3.13"

      # 3️⃣ Install Poetry
      - name: Install Poetry
        run: pip install poetry

      

      #  Set version dynamically (optional but recommended)
      # WHY: Ensures unique version per build
      - name: Set version from Git tag or run number
        run: |
          poetry version 1.0.${{ github.run_number }}

      # 6️⃣ Build wheel
      - name: Build wheel
        run: poetry build

      # 7️⃣ Publish to private PyPI
      - name: Publish to Private PyPI
        run: |
          poetry config repositories.private-pypi ${{ secrets.PRIVATE_PYPI_URL }}
          poetry config pypi-token.private-pypi ${{ secrets.PRIVATE_PYPI_TOKEN }}
          poetry publish -r private-pypi

      # 8️⃣ Upload wheel as artifact (used by deploy jobs)
      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheel
          path: dist/*.whl

  deploy-airflow:
    name: Deploy to Airflow
    needs: build
    runs-on: ubuntu-latest
    environment: production   # requires approval

    steps:
      # 9️⃣ Download built wheel
      - name: Download wheel
        uses: actions/download-artifact@v4
        with:
          name: wheel

      # 🔟 Setup SSH
      - name: Setup SSH
        run: |
          echo "${{ secrets.AIRFLOW_SSH_KEY }}" > key.pem
          chmod 600 key.pem

      # 1️⃣1️⃣ Copy wheel to the user's home directory (Works for Linux & Windows)
      - name: Copy wheel to server home
        run: |
          # The ':' at the end targets the user's default home folder
          scp -i key.pem *.whl \
          ${{ secrets.AIRFLOW_USER }}@${{ secrets.AIRFLOW_HOST }}:

      # 1️⃣2️⃣ Verify the file exists (Cross-platform check)
      - name: Verify file on server
        run: |
          ssh -i key.pem \
          ${{ secrets.AIRFLOW_USER }}@${{ secrets.AIRFLOW_HOST }} << 'EOF'
            echo "Searching for uploaded wheel..."
            # 'ls' works on Linux/Ubuntu and Windows OpenSSH (Git Bash)
            # If it's pure Windows CMD, 'dir' would be used, but 'ls' is usually available in SSH
            ls -lh *.whl || dir *.whl
          EOF


